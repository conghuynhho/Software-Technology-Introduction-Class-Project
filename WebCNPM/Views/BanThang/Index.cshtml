@model IEnumerable<WebCNPM.Models.BANTHANG>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BanThang1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BanThang1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiBanThang }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiBanThang }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiBanThang })
        </td>
    </tr>
}

</table>
